{
  "type": "module",
  "scripts": {
    "db:migrate:local": "wrangler d1 migrations apply ashes-5-0 --local",
    "db:migrations:list:local": "wrangler d1 migrations list ashes-5-0 --local",
    "dev": "wrangler pages dev site",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
