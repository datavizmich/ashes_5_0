{
  "type": "module",
  "scripts": {
    "db:migrate:local": "wrangler d1 migrations apply ashes-5-0 --local --persist-to /tmp/ashes-5-0-wrangler",
    "db:migrations:list:local": "wrangler d1 migrations list ashes-5-0 --local --persist-to /tmp/ashes-5-0-wrangler",
    "dev": "wrangler pages dev site --persist-to /tmp/ashes-5-0-wrangler",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
